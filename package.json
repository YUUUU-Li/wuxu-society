{
  "name": "wuxu-literary-society",
  "version": "2.0.0",
  "private": true,
  "description": "婺需文学社官方网站源码 (Eleventy 静态站点)",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "test": "npm run build && node scripts/test-submit.js && node scripts/test-zhuzhu.js && node scripts/test-related.js && node scripts/verify-dist.js",
    "preview": "python scripts/preview_server.py",
    "seal-issue": "node scripts/seal-issue.js",
    "remove-work": "node scripts/remove-work.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}